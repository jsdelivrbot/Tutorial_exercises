{
  "name": "server",
  "version": "1.0.0",
  "description": "basic server to serve JSON and CSV for d3 testing",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "charlie storey",
  "license": "ISC",
  "dependencies": {
    "express": "^4.14.0"
  }
}
